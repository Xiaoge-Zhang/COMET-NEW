% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation_parallelization.R
\name{simulation_parallelization}
\alias{simulation_parallelization}
\title{Run Multiple COMET Simulations (Optionally in Parallel)}
\usage{
simulation_parallelization(
  n_runs,
  seeds = NULL,
  parallel = FALSE,
  workers = NULL,
  days = 730,
  can_start = 1200,
  match_alg = match_cas,
  ...,
  desired = "random",
  return_params = FALSE,
  include_matches = FALSE
)
}
\arguments{
\item{n_runs}{Integer. Number of independent simulation runs.}

\item{seeds}{Optional integer vector of length \code{n_runs}.
Each element specifies the random seed used for one run.
If \code{NULL}, seeds are generated internally.}

\item{parallel}{Logical. If \code{TRUE}, simulations are executed
in parallel using \pkg{future.apply}. If \code{FALSE}, runs are
executed sequentially.}

\item{workers}{Integer. Number of parallel workers to use when
\code{parallel = TRUE}. Defaults to the minimum of
\code{n_runs} and the number of available CPU cores.}

\item{days}{Integer. Number of days to simulate.}

\item{can_start}{Integer. Initial number of candidates in the waitlist.}

\item{match_alg}{Function. Matching algorithm used by
\code{\link{run_simulation}} (e.g., \code{match_cas}).}

\item{...}{Additional arguments passed directly to
\code{\link{run_simulation}}.}

\item{desired}{Character. Desired match strategy passed to
\code{\link{run_simulation}}.}

\item{return_params}{Logical. If \code{TRUE}, model parameters
are returned alongside simulation results.}

\item{include_matches}{Logical. If \code{TRUE}, detailed match
records are included in the output.}
}
\value{
A \code{tibble} with one row per simulation run, containing:
\itemize{
  \item \code{run_id}: Integer run index.
  \item \code{seed}: Random seed used for the run.
  \item \code{result}: Output object returned by
    \code{\link{run_simulation}}.
}
}
\description{
This function executes multiple independent runs of
\code{\link{run_simulation}} using distinct random seeds.
Runs may be executed sequentially or in parallel via
the \pkg{future} framework.
}
\seealso{
\code{\link{run_simulation}}
}
